{
  "name": "rosiva-awin-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone script that syncs the Awin product feed into Firestore. Runs from GitHub Actions (see .github/workflows/awin-sync.yml), not Firebase Cloud Functions, so it works on the Firebase Spark (free) plan.",
  "scripts": {
    "sync": "node awinSync.mjs"
  },
  "dependencies": {
    "csv-parse": "^5.6.0",
    "firebase-admin": "^13.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
